{
  "configurations": [
        {
            "type": "node",
            "request": "launch",
            "name": "Launch Program",
            "skipFiles": [
                "<node_internals>/**"
            ],
            "program": "${workspaceFolder}/${fileBasename}",
            "runtimeExecutable": "/usr/local/bin/node" // 根据实际路径修改
        }
    ]
}